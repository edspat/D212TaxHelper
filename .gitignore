# Dependencies
node_modules/

# Python (PaddleOCR portable environment)
python/
_python_temp/

# Update staging
_update/

# Uploads (user PDFs)
uploads/

# Logs
logs/

# Raw parsed data (personal financial data)
data/*_raw.txt
data/parsed_data.json
data/trades.json
data/stock_awards.json
data/ledger.json
data/personal_data.json
data/d212tax.db
data/d212tax.db-shm
data/d212tax.db-wal

# Local-only PII used for anonymization regression tests — never commit
test/fixtures/banned-tokens.local.json
# Imported DUF XML files may contain CNP/IBAN/PFA details — never commit
*.duf-import.xml
data/*duf*.xml

# Keep pdf_metadata.json (document type definitions)
!data/pdf_metadata.json

# OS files
Thumbs.db
.DS_Store

# Editor
.vscode/
*.swp
*.swo

# Temporary commit message scratch file
_commit_msg.txt

